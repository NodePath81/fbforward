# Test baseline — 2026-07-10

This baseline was captured before the Flow domain-model refactor.

## Repository state

- Historical Git HEAD: `af4bb07`
- Historical initial `git status --short`: clean
- The current Phase 10 health-model fixture SHA-256 is:
  `fe3004da1a07df8c25605e1aa15d652b3f76f7392c4de39d7e0c16c8a23e6461`

## Commands

```bash
go test ./...
npm --prefix fbcoord test
npm --prefix web run build
```

## Results

- `go test ./...`: passed for all packages. The existing sqlite3 generated C
  binding emitted a compiler warning about discarding a `const` qualifier; it
  did not fail the test run.
- `npm --prefix fbcoord test`: passed, 11 test files and 80 tests.
- `npm --prefix web run build`: passed with Vite production output.

The normalized Phase 10 configuration sample is preserved as the byte-for-byte
fixture at `internal/config/testdata/config.example.yaml`.
