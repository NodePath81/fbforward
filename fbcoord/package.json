{
  "name": "fbcoord",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:worker && npm run build:ui",
    "build:worker": "tsc -p tsconfig.json --noEmit",
    "build:ui": "node ./scripts/prepare-ui-dist.mjs && tsc -p tsconfig.ui.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
